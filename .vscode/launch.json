{
  "configurations": [
    {
      "name": "gateway",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/gateway/cmd",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}